<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booking Confirmed - {{ booking.business.name }}</title>
  <link rel="preconnect" href="https://fonts.bunny.net" />
  <link href="https://fonts.bunny.net/css?family=instrument-sans:400,500,600,700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --sand-1: #fdfdfc;
      --sand-2: #f9f9f8;
      --sand-3: #f1f0ef;
      --sand-4: #e9e8e6;
      --sand-5: #e2e1de;
      --sand-6: #dad9d6;
      --sand-7: #cfceca;
      --sand-8: #bcbbb5;
      --sand-9: #8d8d86;
      --sand-10: #82827c;
      --sand-11: #63635e;
      --sand-12: #21201c;
      --success: #10b981;
      --error: #ef4444;
      --warning: #f59e0b;
    }
  </style>

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ["Instrument Sans", "sans-serif"]
          },
          colors: {
            primary: {
              DEFAULT: "#5A45FF",
              lighter: "#a599ff"
            },
            sand: {
              1: "var(--sand-1)",
              2: "var(--sand-2)",
              3: "var(--sand-3)",
              4: "var(--sand-4)",
              5: "var(--sand-5)",
              6: "var(--sand-6)",
              7: "var(--sand-7)",
              8: "var(--sand-8)",
              9: "var(--sand-9)",
              10: "var(--sand-10)",
              11: "var(--sand-11)",
              12: "var(--sand-12)"
            },
            success: "var(--success)",
            error: "var(--error)",
            warning: "var(--warning)"
          }
        }
      }
    };
  </script>
</head>
<body class="bg-sand-2 text-sand-12 min-h-screen font-sans">
  <div class="max-w-md mx-auto px-4 py-8">
    {{-- Success Icon --}}
    <div class="text-center mb-8">
      <div class="w-20 h-20 rounded-full bg-success/10 flex items-center justify-center mx-auto mb-4">
        <svg class="w-10 h-10 text-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
      </div>
      <h1 class="text-2xl font-bold text-sand-12">Booking Confirmed!</h1>
      <p class="text-sand-11 mt-2">Your appointment has been successfully booked.</p>
    </div>

    {{-- Booking Details --}}
    <div class="bg-white border border-sand-7 rounded-xl p-6 mb-6">
      <div class="text-center pb-4 border-b border-sand-6">
        <div class="w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center mx-auto mb-2">
          <span class="text-lg font-bold text-primary">{{ booking.business.name.charAt(0) }}</span>
        </div>
        <h2 class="font-semibold text-sand-12">{{ booking.business.name }}</h2>
      </div>

      <div class="pt-4 space-y-4">
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-sand-11 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
          </svg>
          <div>
            <p class="text-sm text-sand-11">Service</p>
            <p class="font-medium text-sand-12">{{ booking.service.name }}</p>
          </div>
        </div>

        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-sand-11 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
          </svg>
          <div>
            <p class="text-sm text-sand-11">Date & Time</p>
            <p class="font-medium text-sand-12">{{ booking.date.toFormat('EEEE, MMMM d, yyyy') }}</p>
            <p class="text-sand-12">{{ booking.startTime }} - {{ booking.endTime }}</p>
          </div>
        </div>

        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-sand-11 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <div>
            <p class="text-sm text-sand-11">Amount Paid</p>
            <p class="font-medium text-sand-12">₦{{ Number(booking.amount).toLocaleString() }}</p>
          </div>
        </div>
      </div>
    </div>

    {{-- Reference --}}
    <div class="bg-sand-3 rounded-lg p-4 text-center mb-6">
      <p class="text-sm text-sand-11">Booking Reference</p>
      <p class="font-mono font-medium text-sand-12">{{ booking.paymentReference.substring(0, 8).toUpperCase() }}</p>
    </div>

    {{-- Info --}}
    <div class="bg-primary/5 border border-primary/20 rounded-xl p-4 mb-6">
      <p class="text-sm text-sand-12">
        <strong>What's next?</strong><br/>
        A confirmation email has been sent to <span class="font-medium">{{ booking.customerEmail }}</span>.
        Please arrive 5 minutes before your scheduled time.
      </p>
    </div>

    {{-- Actions --}}
    <div class="space-y-3">
      <a
        href="/book/{{ booking.business.slug }}"
        class="block w-full py-3 text-center bg-primary text-white font-medium rounded-lg hover:bg-primary/90"
      >
        Book Another Appointment
      </a>
    </div>

    {{-- Footer --}}
    <div class="mt-12 text-center text-sm text-sand-10">
      Powered by <a href="/" class="text-primary hover:underline">BookMe</a>
    </div>
  </div>
</body>
</html>
