@layout('layouts/app')

@set('title', 'Add Portfolio Item - FastAppoint')

@section('content')
  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    @if(flashMessages.has('error'))
      <div class="mb-6 bg-error-bg border-l-4 border-l-error text-error rounded-xl p-4 shadow-sm">
        <div class="flex items-center gap-3">
          <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <span class="font-semibold">{{ flashMessages.get('error') }}</span>
        </div>
      </div>
    @endif

    <div class="mb-6">
      <a href="{{ route('portfolio.index') }}" class="text-sm text-primary hover:text-primary-dark font-semibold inline-flex items-center gap-2 transition-colors">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
        </svg>
        Back to Portfolio
      </a>
    </div>

    <div class="card shadow-lg">
      <div class="p-6 border-b border-neutral-200">
        <h1 class="text-2xl font-bold text-neutral-900">Add Portfolio Item</h1>
        <p class="text-sm text-neutral-600 mt-1">Showcase your work to potential clients</p>
      </div>

      <form method="POST" action="{{ route('portfolio.store') }}" enctype="multipart/form-data" class="p-6 space-y-6">
        {{ csrfField() }}

        <div>
          <label class="block text-sm font-medium text-neutral-900 mb-2">
            Image <span class="text-error">*</span>
          </label>
          <div
            x-data="{
              preview: null,
              handleFile(e) {
                const file = e.target.files[0]
                if (file) {
                  this.preview = URL.createObjectURL(file)
                }
              }
            }"
            class="space-y-3"
          >
            <div
              class="border-2 border-dashed border-neutral-200 rounded-lg p-6 text-center hover:border-primary transition-colors cursor-pointer"
              @click="$refs.fileInput.click()"
            >
              <template x-if="!preview">
                <div>
                  <svg class="w-12 h-12 mx-auto text-neutral-500 mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                  </svg>
                  <p class="text-sm text-neutral-600 mb-1">Click to upload an image</p>
                  <p class="text-xs text-neutral-500">JPG, PNG or WebP (max 5MB)</p>
                </div>
              </template>
              <template x-if="preview">
                <div class="relative">
                  <img :src="preview" class="max-w-full max-h-64 object-contain rounded-lg mx-auto" />
                  <button
                    type="button"
                    @click.stop="preview = null; $refs.fileInput.value = ''"
                    class="absolute top-2 right-2 bg-error text-white rounded-full p-1"
                  >
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                    </svg>
                  </button>
                </div>
              </template>
            </div>
            <input
              type="file"
              name="image"
              x-ref="fileInput"
              @change="handleFile($event)"
              accept="image/jpeg,image/png,image/webp"
              class="hidden"
              required
            />
          </div>
        </div>

        <div>
          <label for="title" class="block text-sm font-medium text-neutral-900 mb-2">
            Title <span class="text-error">*</span>
          </label>
          <input
            id="title"
            name="title"
            type="text"
            required
            class="w-full px-4 py-3 input-field"
            placeholder="e.g., Bridal Makeup, Portrait Session"
          />
        </div>

        <div>
          <label for="description" class="block text-sm font-medium text-neutral-900 mb-2">
            Description <span class="text-neutral-500">(optional)</span>
          </label>
          <textarea
            id="description"
            name="description"
            rows="3"
            class="w-full px-4 py-3 input-field"
            placeholder="Describe this work..."
          ></textarea>
        </div>

        <div>
          <label for="serviceId" class="block text-sm font-medium text-neutral-900 mb-2">
            Related Service <span class="text-neutral-500">(optional)</span>
          </label>
          <select
            id="serviceId"
            name="serviceId"
            class="w-full px-4 py-3 input-field"
          >
            <option value="">No specific service</option>
            @each(service in services)
              <option value="{{ service.id }}">{{ service.name }}</option>
            @endeach
          </select>
          <p class="text-xs text-neutral-500 mt-1">Link this work to a service you offer</p>
        </div>

        <div>
          <label class="flex items-center gap-3 cursor-pointer">
            <input
              type="checkbox"
              name="isFeatured"
              value="true"
              class="w-5 h-5 rounded border-neutral-200 text-primary focus:ring-primary"
            />
            <div>
              <span class="text-sm font-medium text-neutral-900">Mark as Featured</span>
              <p class="text-xs text-neutral-500">Featured items appear prominently on your booking page</p>
            </div>
          </label>
        </div>

        <div class="pt-4 flex gap-3">
          <button type="submit" class="btn-primary">
            Add to Portfolio
          </button>
          <a href="{{ route('portfolio.index') }}" class="btn-ghost">
            Cancel
          </a>
        </div>
      </form>
    </div>
  </div>
@endsection
