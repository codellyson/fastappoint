@layout('layouts/app')

@set('title', 'Sign Up - FastAppoint')

@section('content')
  <div class="min-h-screen flex items-center justify-center py-8 sm:py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full">
      <div class="text-center mb-6 sm:mb-8">
        <h2 class="text-xl sm:text-2xl font-bold text-sand-12">Get started in 2 minutes</h2>
        <p class="mt-2 text-xs sm:text-sm text-sand-11">
          Create your account and start accepting bookings
        </p>
      </div>

      @if(flashMessages.has('error'))
        <div class="mb-6 bg-error/10 border border-error text-error rounded-lg p-4 text-sm">
          {{ flashMessages.get('error') }}
        </div>
      @endif

      @if(flashMessages.has('errors'))
        <div class="mb-6 bg-error/10 border border-error text-error rounded-lg p-4 text-sm">
          <ul class="list-disc list-inside">
            @each(err in flashMessages.get('errors'))
              <li>{{ err.message }}</li>
            @endeach
          </ul>
        </div>
      @endif

      <form
        class="bg-white border border-sand-7 rounded-xl p-4 sm:p-6 space-y-4 sm:space-y-5"
        method="POST"
        action="{{ route('auth.signup') }}"
        x-data="{ loading: false }"
        @submit="loading = true"
      >
        {{ csrfField() }}

        <div>
          <label for="fullName" class="block text-xs sm:text-sm font-medium text-sand-12 mb-1.5">
            Your Name
          </label>
          <input
            id="fullName"
            name="fullName"
            type="text"
            required
            value="{{ flashMessages.get('fullName') || '' }}"
            class="w-full px-3 sm:px-4 py-2.5 border border-sand-7 rounded-lg placeholder-sand-9 text-sm sm:text-base text-sand-12 focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary min-h-[44px]"
            placeholder="John Doe"
          />
          @error('fullName')
            <p class="mt-1 text-xs sm:text-sm text-error">{{ $message }}</p>
          @enderror
        </div>

        <div>
          <label for="email" class="block text-xs sm:text-sm font-medium text-sand-12 mb-1.5">
            Email
          </label>
          <input
            id="email"
            name="email"
            type="email"
            required
            autocomplete="email"
            class="w-full px-3 sm:px-4 py-2.5 border border-sand-7 rounded-lg placeholder-sand-9 text-sm sm:text-base text-sand-12 focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary min-h-[44px]"
            placeholder="john@example.com"
          />
          @error('email')
            <p class="mt-1 text-xs sm:text-sm text-error">{{ $message }}</p>
          @enderror
        </div>

        <div>
          <label for="password" class="block text-xs sm:text-sm font-medium text-sand-12 mb-1.5">
            Password
          </label>
          <input
            id="password"
            name="password"
            type="password"
            required
            autocomplete="new-password"
            class="w-full px-3 sm:px-4 py-2.5 border border-sand-7 rounded-lg placeholder-sand-9 text-sm sm:text-base text-sand-12 focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary min-h-[44px]"
            placeholder="At least 8 characters"
          />
          @error('password')
            <p class="mt-1 text-xs sm:text-sm text-error">{{ $message }}</p>
          @enderror
        </div>

        <div class="border-t border-sand-6 pt-4 sm:pt-5">
          <p class="text-xs font-medium text-sand-11 uppercase tracking-wider mb-3 sm:mb-4">Your Business</p>
          
          <div class="space-y-4 sm:space-y-5">
            <div>
              <label for="businessName" class="block text-xs sm:text-sm font-medium text-sand-12 mb-1.5">
                Business Name
              </label>
              <input
                id="businessName"
                name="businessName"
                type="text"
                required
                class="w-full px-3 sm:px-4 py-2.5 border border-sand-7 rounded-lg placeholder-sand-9 text-sm sm:text-base text-sand-12 focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary min-h-[44px]"
                placeholder="Glow Beauty Salon"
              />
              @error('businessName')
                <p class="mt-1 text-xs sm:text-sm text-error">{{ $message }}</p>
              @enderror
            </div>

            <div>
              <label for="category" class="block text-xs sm:text-sm font-medium text-sand-12 mb-1.5">
                Category
              </label>
              <select
                id="category"
                name="category"
                required
                class="w-full px-3 sm:px-4 py-2.5 border border-sand-7 rounded-lg text-sm sm:text-base text-sand-12 focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary bg-white min-h-[44px]"
              >
                <option value="">Choose a category</option>
                <option value="beauty">💇 Beauty & Wellness</option>
                <option value="media">📸 Photography & Media</option>
                <option value="health">🏥 Health & Medical</option>
                <option value="fitness">💪 Fitness & Sports</option>
                <option value="education">📚 Education & Coaching</option>
                <option value="other">🏢 Other Services</option>
              </select>
              @error('category')
                <p class="mt-1 text-xs sm:text-sm text-error">{{ $message }}</p>
              @enderror
            </div>
          </div>
        </div>

        <button
          type="submit"
          :disabled="loading"
          class="w-full py-3 px-4 text-sm sm:text-base font-medium rounded-lg text-white bg-primary hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary disabled:opacity-50 disabled:cursor-not-allowed transition-colors min-h-[44px]"
        >
          <span x-show="!loading">Create Account</span>
          <span x-show="loading" class="flex items-center justify-center gap-2">
            <svg class="animate-spin h-5 w-5" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" fill="none"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            Creating...
          </span>
        </button>
      </form>

      <p class="mt-4 sm:mt-6 text-center text-xs sm:text-sm text-sand-11">
        Already have an account?
        <a href="{{ route('auth.login.show') }}" class="font-medium text-primary hover:text-primary/80">
          Sign in
        </a>
      </p>
    </div>
  </div>
@endsection
